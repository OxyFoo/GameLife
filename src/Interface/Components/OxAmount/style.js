import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    parent: {
        flexDirection: 'row',
        alignItems: 'center'
    },
    icon: {
        marginLeft: 4
    }
});

export default styles;
