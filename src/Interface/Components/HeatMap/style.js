import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
      flexDirection: 'row',
      flexWrap: 'wrap',
    },
    week: {
      width: 10, // Width of the week cell
      height: 10, // Height of the week cell
      margin: 2, // Margin around the week cell
    },
  });

export default styles;