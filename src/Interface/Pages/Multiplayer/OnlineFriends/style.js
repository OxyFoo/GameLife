import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    flatList: {
        marginTop: 12,
        marginBottom: 64
    },
    item: {
        marginBottom: 6
    }
});

export default styles;
