# @oxyfoo/* packages are published to GitHub Packages, not to the public registry.
# Authentication: add a token with the read:packages scope to your ~/.npmrc
#   //npm.pkg.github.com/:_authToken=<TOKEN>
# The CI workflows rewrite this file with the CICD_PAT secret.
@oxyfoo:registry=https://npm.pkg.github.com/
