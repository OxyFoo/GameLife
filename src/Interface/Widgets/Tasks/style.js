import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    tasksContainer: {
        padding: 28,
        paddingTop: 14
    },
    emptyText: {
        marginBottom: 12
    }
});

export default styles;