import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    todosContainer: {
        padding: 28,
        paddingTop: 14
    },
    emptyText: {
        marginBottom: 12
    }
});

export default styles;
