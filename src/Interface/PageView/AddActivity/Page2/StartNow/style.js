import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    title: {
        marginTop: 32,
        marginBottom: 18,
        fontSize: 24,
        fontWeight: 'bold'
    },
    startNowButton: {
        marginBottom: 24
    }
});

export default styles;
