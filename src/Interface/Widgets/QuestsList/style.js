import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    questsContainer: {
        padding: 0
    },
    emptyText: {
        marginBottom: 12
    }
});

export default styles;
