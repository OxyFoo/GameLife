import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    avatarContainer: {
        position: 'absolute',
        top: 100,
        zIndex: -100,
        elevation: -100
    }
});

export default styles;
