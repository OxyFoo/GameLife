import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    switchText: {
        marginBottom: 12
    },
    headerText: {
        fontSize: 16,
        fontWeight: 'bold'
    }
});

export default styles;
