import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    questsContainer: {
        padding: 0
    },
    emptyText: {
        padding: 24
    },

    separator: {
        width: 'auto',
        height: 1.5,
        marginHorizontal: 12,
        opacity: .5
    }
});

export default styles;
